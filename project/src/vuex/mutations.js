export const setTitle = (state,param) => {
    state.title = param;
}

export const setBatch = (state,param) => {
    state.batchData = param;
}

export const setSearch = (state,param) => {
    state.searchData = param;
}
